# Registry

The registry component of Scrubber maintains a catalogue of registered functions that can be imported individually as needed. The registry enables the functions to be referenced by name using string values. The code registry is created and accessed using the [catalogue](https://github.com/explosion/catalogue) library by Explosion.

### ::: lexos.scrubber.registry
