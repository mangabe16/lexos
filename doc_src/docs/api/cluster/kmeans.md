# KMeans

The `kmeans` module performs k-means clustering.

## The `KMeans` Class

::: lexos.cluster.kmeans
    handler: python
    selection:
      members:
        - KMeans
        - KMeans.__init__
        - KMeans.fit
        - KMeans.predict
        - KMeans.labels_
        - KMeans.cluster_centers_
        - kmeans
